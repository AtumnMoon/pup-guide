import { define } from "@/utils.ts";

export default define.page(() => {
  // TODO: Introduce "us"
  //       - Tell the reason why we created this
  //       - Tell things about PUP
  return <p>About</p>;
});
